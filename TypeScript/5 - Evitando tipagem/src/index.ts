// function sendSpaceship (spaceship : {pilot : string, copilot? : string}) { //O ponto de interrogacao informa que não é um parametro obrigatorio
//   //...
// }

// sendSpaceship({pilot: 'Han Solo', copilot: 'Chewbacca'})

// sendSpaceship({pilot: 'Luke'})

let input : any // o unknow apenas informa que o tipo é desconhecido, já o any desativa o typescript, e atua como o javascript

input = 'Test'
input = 20